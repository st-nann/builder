export enum EDirection {
    ROW = 'row',
    COLUMN = 'column'
}

export enum EJustify {
    START = 'start',
    CENTER = 'center',
    END = 'end'
}

export enum EAlign {
    START = 'start',
    CENTER = 'center',
    END = 'end'
}

export enum EInputType {
    TEXT = 'text',
    PASSWORD = 'password',
    EMAIL = 'email',
    NUMBER = 'number'
}
